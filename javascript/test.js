class  Tree {
    constructor() {
        this.root = null;
    }

    insert (key) {
        // let newNode = new Node(key);

        console.log(key)
    }
}


let tree = new Tree();
tree.insert('hello, world');
